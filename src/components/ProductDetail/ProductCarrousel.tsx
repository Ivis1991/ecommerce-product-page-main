import React from "react";
import {
  AlertDialog,
  AlertDialogAction,
  AlertDialogCancel,
  AlertDialogContent,
  AlertDialogDescription,
  AlertDialogFooter,
  AlertDialogHeader,
  AlertDialogTitle,
  AlertDialogTrigger,
} from "../ui/alert-dialog";

type Props = {};

export const ProductCarrousel = (props: Props) => {
  return (
    <div>
      <AlertDialog>
        <AlertDialogContent>Test</AlertDialogContent>
      </AlertDialog>
    </div>
  );
};
